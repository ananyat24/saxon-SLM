# mock (default) uses in-memory fixtures; live calls a real backend proxy.
VITE_API_MODE=mock

# Base URL of the backend proxy when VITE_API_MODE=live.
# Never point this at a service that requires an API key in the frontend —
# the proxy is responsible for holding credentials.
VITE_API_BASE_URL=https://api.example.com
